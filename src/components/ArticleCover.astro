---
interface Props {
  title: string;
  event?: string;
  section?: string;
  year?: number;
  author?: string;
  role?: string;
  project?: string;
  transitionName?: string;
}
const { title, event, section, year, author, role, project, transitionName } = Astro.props;
const hasYear = typeof year === 'number';
const kicker = event
  ? event + (hasYear && !event.includes(String(year)) ? ` · ${year}` : '')
  : [section, hasYear ? String(year) : ''].filter(Boolean).join(' · ');
---

<header class="cover" style={transitionName ? `view-transition-name: ${transitionName}` : undefined}>
  {kicker && <div class="kicker">{kicker}</div>}
  <h1>{title}</h1>
  <div class="meta">
    {author && <span class="chip">👤 {author}</span>}
    {role && <span class="chip">🎨 {role}</span>}
    {project && <span class="chip">🎮 {project}</span>}
  </div>
</header>

<style>
  .cover {
    position: relative;
    border-radius: var(--r-card);
    overflow: hidden;
    padding: 36px 34px 34px;
    color: #fff;
    background:
      radial-gradient(120% 140% at 100% 0%, rgba(255, 255, 255, 0.12), transparent 55%),
      var(--cover);
    box-shadow: 0 18px 40px rgba(13, 58, 140, 0.25);
  }
  .kicker {
    font-size: 13px;
    letter-spacing: 1.5px;
    opacity: 0.7;
    margin-bottom: 14px;
  }
  .cover h1 {
    font-family: var(--font-serif);
    font-size: 28px;
    font-weight: 700;
    line-height: 1.35;
    margin: 0 0 16px;
    letter-spacing: 0.5px;
  }
  .meta {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .chip {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    font-size: 12.5px;
    padding: 5px 12px;
    border-radius: var(--r-pill);
    background: rgba(255, 255, 255, 0.14);
    border: 1px solid rgba(255, 255, 255, 0.22);
  }
</style>
